version: '2.4'

services:
  bref:
    build:
      context: .
    ports:
      - 8333:8080
    environment:
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_DEFAULT_REGION
    volumes:
      - ./:/app:delegated

networks:
  default:
    external: true
    name: local
